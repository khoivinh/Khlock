# 2026-04-20 Devlog — Happyhour rebrand: Tier 1 + Tier 2 + Tier 3 walkthrough + Phase A cleanup + Phase B infra renames

## What changed

Executed **Tier 1** of the Happyhour rebrand + enhancement plan (`.claude/plans/i-managed-to-mellow-church.md`). Source spec: `docs/2026-04-20-happyhour-edits.md` (renamed from the original `-khlock-edits.md`). Tier 1 is the autonomous slice — no Figma assets or external dashboard actions required.

### 1. Rebrand plumbing (strings + storage keys)

| File | Change |
|---|---|
| `package.json` | `"name": "khlock"` → `"happyhour"` |
| `api/package.json` | `"name": "khlock-api"` → `"happyhour-api"` |
| `api/wrangler.jsonc` | Worker name renamed; `happyhour.day` added to CORS allowlist (kept `khlock.pages.dev` as transitional fallback). D1 database name kept as `khlock-db` per plan (internal, disruptive to rename). |
| `client/index.html` | `<title>Khlock</title>` → `<title>Happyhour</title>` |
| `client/src/pages/world-clock.tsx` | Header text `Khlock` → `Happyhour` (placeholder; Tier 2 will swap in the Figma logo treatment). LocalStorage keys renamed to `world-happyhour-*`. |
| `client/src/components/time-zone-converter.tsx` | `STORAGE_KEY` → `world-happyhour-zones` |
| `client/src/hooks/use-cloud-sync.ts` | Sync snapshot + timestamp keys → `world-happyhour-*` |
| `client/src/lib/theme-provider.tsx` | Theme storage key → `world-happyhour-theme` |

**Data decision:** fresh start, no migration. Existing users lose local tiles on next visit and re-initialize to defaults (Tokyo / New York / Paris). D1 cloud-synced data is unaffected — the binding still points at `khlock-db`.

### 2. UX enhancements

- **Scroll-to-current-city on edit** (`digital-clock.tsx`): Added a `useEffect` that runs when the city popover opens, finds the selected `CommandItem` by its `data-testid`, and calls `scrollIntoView({ block: "center" })`. Also guarded the filter list so the currently-selected city is always present even when truncated to 100 results, otherwise scroll-to would silently fail for cities outside the default slice (there are 30k+ cities in the dataset).
- **Native `confirm()` replaced with shadcn Dialog** (`digital-clock.tsx`): The browser's native `confirm("Remove X?")` was rendering as `khlock.pages.dev says: Remove newYork_US?` — now renders as a shadcn `<Dialog>` titled "Happyhour" with `Cancel` / `Remove` actions. ESC + outside-click close via Radix defaults.
- **Badge typography** (`digital-clock.tsx`, desktop + mobile): Next / Prev / Offset badges moved from `text-[7px] font-bold leading-[15px]` to `text-[8px] font-semibold leading-[16px]`. Added `tracking-[0.2px]` to the Next/Prev spans only (per spec the offset badge has no tracking).
- **Clock tile removal animation** (`digital-clock.tsx`): Introduced an `isRemoving` state; when the user confirms removal the tile gets `animate-out fade-out-0 zoom-out-95 duration-200`, then a 200ms `setTimeout` calls the parent's `onRemove` to actually remove the tile from the list. Drops the previous abrupt disappearance.

### 3. Bug fixes

- **Z-index on multi-word city (`Los Angeles`, etc.)**: Bumped the city-selector `PopoverContent` className from implicit `z-50` to `z-[60]` so it always sits above the active-edit tile's yellow-highlight state.
- **Default logged-out second tile showing "Select City"**: Code already specified `["tokyo_JP", "newYorkCity_US", "paris_FR"]`. The bug was stale `newYork_US` in localStorage — the fresh `world-happyhour-*` key prefix resolves it automatically. No code change needed.
- **Idle data-clearing**: Added `visibilitychange` + `focus` listeners on `TimeZoneConverter` that force-refresh `currentTime` when the tab returns to the foreground. Standalone `useEffect` so it doesn't tear down with drag activity or custom-mode toggles.
- **Closest city targets Los Angeles for Northern CA**: Added `client/src/lib/closest-city.ts` with a haversine-distance search over all ~30k cities. On mount, after setting the timezone-derived fallback, calls `navigator.geolocation.getCurrentPosition` and upgrades `heroZone` to the geographically-nearest city. Cached for 24h in `world-happyhour-local-city`. Silent — denial/timeout stays on the old timezone heuristic, so no regression for users who deny permission.
- **Drawer toggle alignment (open vs closed)**: Removed `mt-[2px]` from the sidebar's internal close button so it aligns with the page-header toggle. Both `DrawerToggleIcon` SVGs already share the same viewBox + rect coords; the misalignment came from this one offset class. Tier 2 will swap in the redrawn (thicker-stroke) icons from Figma but the geometry stays consistent.

## Verification

- `npx tsc --noEmit` — clean (exit 0)
- `npm run build` — clean build, `happyhour@1.0.0` header visible in output
- Manual browser verification **not yet done** — user to smoke-test `npm run dev` and confirm.

## What's next (as of Tier 1 completion)

- **Tier 2** (Figma MCP), **Tier 3** (external walkthrough), **Phase A** (doc cleanup), and **Phase B** (infra renames) all landed same day — see below.

---

## Tier 2 — Figma-assisted asset work (landed same day)

Pulled all referenced Figma nodes via authenticated MCP and used the matching production-ready SVGs already checked in at `design/User Interface Designs/` and `design/Brand/`.

### Changes

| File | Change |
|---|---|
| `client/public/favicon.svg` (new) | Black rounded square + yellow smiley-clock logo (copied from `design/User Interface Designs/favicon.svg`, 512×512) |
| `client/public/favicon.png` (removed) | Old Khlock rainbow PNG deleted — modern browsers handle SVG favicons natively, so the PNG fallback was only serving the old branding |
| `client/public/og.png` (new) | 1200×630 resized from `design/User Interface Designs/happyhour-opengraph.png` via `sips`, 94 KB |
| `client/index.html` | Added `og:*`, `twitter:*`, and `apple-touch-icon` meta tags; `og:image` points at `https://happyhour.day/og.png` (absolute URL required by scrapers); description refreshed to "The indispensable world clock. It's always that time somewhere." (matches OG copy) |
| `client/src/components/icons/drawer-closed.tsx` (new) | Inline React SVG, fills replace strokes — matches Figma node 95-2575 closed state |
| `client/src/components/icons/drawer-open.tsx` (new) | Inline React SVG with right-pointing arrow — matches Figma 95-2575 open state |
| `client/src/components/icons/happyhour-logo.tsx` (new) | Yellow smiley with checkmark eye sweep (derived from favicon SVG, without the rounded square background); face features use `currentColor` so they adapt to the theme's foreground |
| `client/src/components/sidebar.tsx` | `DrawerToggleIcon` now just delegates to the new components; close button wrapper bumped from `w-[24px]` → `w-[25px]` to match the new SVG viewBox |
| `client/src/pages/world-clock.tsx` | Added `<HappyhourLogo>` alongside the "Happyhour" wordmark in the header; sized as `w-[0.95em]` so it scales automatically with the existing font-size animation |
| `client/src/index.css` | Added `.happy` palette — `#ffd900` background (HSL `51 100% 50%`), near-black foreground, white cards for contrast, dark sidebar so controls stay legible |
| `client/src/lib/theme-provider.tsx` | Extended `Theme` union + `ResolvedTheme` to include `"happy"`; class-list cleanup now strips all three theme classes before applying the new one |
| `client/src/components/sidebar.tsx` | `cycleTheme` rotation now `system → light → dark → happy → system`; Happy icon is the new smiley logo; label "Happy" |
| `client/src/hooks/use-cloud-sync.ts` + `client/src/lib/api.ts` + `client/src/pages/world-clock.tsx` | Widened `SyncablePreferences.theme` / `CloudPreferences.theme` unions to include `"happy"` |
| `api/src/routes/preferences.ts` | Added `"happy"` to `VALID_THEMES` so the backend accepts it over the sync endpoint |
| `client/src/components/theme-toggle.tsx` (deleted) | Unused component — was the old 3-way toggle, not rendered anywhere. Removed to avoid drift with sidebar's 4-way cycle |
| `client/src/components/digital-clock.tsx` | Removal animation duration bumped from 200ms → 800ms per feedback; swapped `duration-[800ms]` → `[animation-duration:800ms]` to silence a Tailwind ambiguity warning |

### Notes

- Brand color tokens pulled from Figma: Background Yellow `#ffd900`, Logo Mark `#ffcd05`. Both referenced in the new favicon SVG and happyhour-logo component.
- Happy Mode intentionally uses a dark sidebar (same as light mode's dark sidebar) rather than a yellow one — the sidebar's interactive density makes yellow-on-yellow hard to read.
- The scroll-shrink header animation (`world-clock.tsx:42-64`) is untouched; the logo inherits scale via `em`-based sizing, so it shrinks in lock-step with the wordmark.

## Verification (Tier 2)

- `npx tsc --noEmit` — clean
- `npm run build` — clean (final bundle after fix: no warnings, 495 KB main chunk)
- Dev server HMR — all Tier 2 updates applied without reload

## Open for user to verify

- Browser tab favicon (should show black rounded square with yellow smiley)
- OG preview (paste `http://localhost:5173` into a local preview tool; real preview won't work until `happyhour.day` is live since we used absolute URLs)
- Cycle through Appearance — `System → Light → Dark → Happy → System`
- Happy Mode yellow contrast — eyeball the weather colors, tile hover states, and destructive-red Remove button against yellow bg; if anything reads poorly, we tune the palette

## Notes for future sessions (Tier 1)

- Clock tile z-index fix is pragmatic (raise popover to `z-[60]`) rather than diagnostic. If multi-word-city breakage recurs, check whether the sortable wrapper's `transform` is creating an unexpected stacking context when the tile is in edit mode.
- Drawer alignment fix is also pragmatic. If the user reports it's still off by a few pixels, check the `topOffset` calculation in `world-clock.tsx:55–58` against the sidebar's `top: topOffset - 18px` + `pt-[18px]` math — the close button should land at exactly `topOffset.top` with no mt.

---

## Tier 3 — External walkthrough (landed same day, user-driven)

Items the user executed through dashboards while I guided. All logged below for future reference.

| Step | Action | Outcome |
|---|---|---|
| 1 | Added `happyhour.day` as a Cloudflare zone; switched Namecheap nameservers to Cloudflare's pair | Zone went **Active** within minutes |
| 2 | Added `happyhour.day` + `www.happyhour.day` as custom domains on the `khlock` Pages project; set up `www → apex` redirect rule (after removing the template-generated `apex → www` rule that caused a redirect loop) | Custom domain live with SSL |
| 3 | Switched the Clerk production instance's primary domain from `khlock.pages.dev` to `happyhour.day`; added the 5 verification CNAMEs in Cloudflare DNS as **DNS-only (grey cloud)**; verified all 5 in Clerk; captured the new `pk_live_…` publishable key | Clerk production ready on `happyhour.day` |
| 4a | Swapped `VITE_CLERK_PUBLISHABLE_KEY` in Cloudflare Pages env vars (`pk_test_…` → `pk_live_…`) | Pending build picks it up |
| 4b | Committed Tier 1 + Tier 2 code as single commit `100faf2` (23 files, +520/−102) | Tier 1+2 code now in the repo |
| 4c | Pushed to `main` | Cloudflare Pages auto-built successfully |
| 4d | Redeployed the API Worker via `npx wrangler deploy` from `api/` | New CORS allowlist live on the Worker |
| 4d follow-up | Caught that `CLERK_JWKS_URL` still pointed at the dev tenant (`topical-dragon-37.clerk.accounts.dev`) — production tokens would fail token verification on the Worker. Updated `api/wrangler.jsonc` to `https://clerk.happyhour.day/.well-known/jwks.json`, committed as `1b1d2d7`, redeployed | Cloud sync verified end-to-end |

### Decisions along the way

- **Worker name reverted to `khlock-api`** (Tier 1 had renamed it to `happyhour-api`). Renaming forces a new Worker at a new URL + `VITE_API_URL` swap on Pages + old-worker deletion — no user-visible benefit, so I reverted before the commit. Tracked as Phase B.2 if we ever do it.
- **Kept `khlock.pages.dev` in CORS** as a transitional fallback. Since the Pages project can't be renamed in-place, that URL remains live and could be hit by bookmarks/link-unfurlers.
- **Security:** a GitHub PAT in plain text was found in `docs/HANDOFF.md`. Verified the file is gitignored (line 9 of `.gitignore`) and the PAT was never in the git history. Redacted the local file + added a "SUPERSEDED" notice; still recommend revoking the token as a hygiene step.

---

## Phase A — Residual "khlock" cleanup in docs + lockfiles

Commit `ad3cb5a`. Plan: `.claude/plans/i-managed-to-mellow-church.md` (Phase A section). 6 files, +62/−60.

| File | Change |
|---|---|
| `docs/BACKLOG.md` | Title rebranded; completed checkbox added for the 2026-04-20 rebrand |
| `docs/PRD.md` | Title, intro, Figma URLs, iOS tree structure, live URLs, storage key prefix, DB section intro all updated. 2026-04-20 status block added below the 2026-03-26 one. App Rename section re-statused as **Completed** with each sub-item checked off (except the deliberately skipped/deferred items). Historical poll candidate table preserved verbatim (labeled as previous name). |
| `docs/2026-04-20-khlock-edits.md` | Renamed to `docs/2026-04-20-happyhour-edits.md` via `git mv` + in-body `# 2026-04-20-khlock-edits` header updated. |
| `docs/2026-04-20-devlog.md` | Source-spec reference updated to the new filename. |
| `package-lock.json`, `api/package-lock.json` | `"name"` fields regenerated via `npm install` — now show `happyhour` / `happyhour-api` in all 4 slots. |

### Deliberately left alone

- `docs/2026-03-*-khlock-edits.md`, `docs/2026-03-*-devlog.md`, `docs/2026-03-27-naming-poll-results.md`, `docs/Khlock Changes *.md`, `docs/2026-03-24-auth-devlog.md` — dated project-history. Rewriting falsifies the record.
- `api/wrangler.jsonc` infra strings (worker name `khlock-api`, D1 `khlock-db`, CORS fallback) — Phase B targets.
- `api/package.json` db scripts — paired with the D1 rename in Phase B.3.
- `.claude/settings.local.json` — filesystem paths that include "Khlock" because the local folder is still named `Khlock/`. Will self-resolve if/when the user renames the directory.
- `docs/HANDOFF.md` — gitignored; my redaction is local-only and won't be committed.

### Verification (Phase A)

- `grep -ri khlock` over `.ts/.tsx/.json/.jsonc/.html/.css/.md` — remaining matches are exactly the categories listed in "Deliberately left alone" above. No current-tense brand references remain in non-historical source.
- `node node_modules/typescript/bin/tsc --noEmit` → clean. (Note: `npx tsc` currently resolves to the wrong package because `node_modules/.bin/tsc` symlink wasn't recreated by `npm install`; harmless — the build doesn't use it. Can be fixed later with a fresh `npm install` or `rm -rf node_modules && npm install`.)
- `npm run build` → clean, `happyhour@1.0.0` header visible.

---

## What's next — Phase B (same-day follow-up, see below)

Phase B landed the same day. Summary below.

---

## Phase B — Infra renames

All three in-scope items (B.1 GitHub, B.2 Worker, B.3 D1) shipped. B.4 and B.5 deliberately skipped per plan.

### B.1 — GitHub repo rename `Khlock` → `happyhour`

- User renamed via GitHub **Settings → General → Repository name**.
- GitHub auto-redirects the old URL (`github.com/khoivinh/Khlock` → `github.com/khoivinh/happyhour`).
- Updated local git remote: `git remote set-url origin https://github.com/khoivinh/happyhour.git`.
- Verified: `git fetch origin` works cleanly; branch in sync with `origin/main`.
- Cloudflare Pages webhook continued firing from the renamed repo without any reconfiguration (CF tracks repos by internal ID, not URL).

### B.2 — Worker rename `khlock-api` → `happyhour-api`

- Edited `api/wrangler.jsonc` line 2: `"name": "khlock-api"` → `"happyhour-api"`.
- `npx wrangler deploy` from `api/` created a new Worker at `https://happyhour-api.khoi-vinh-s-account.workers.dev`. Old `khlock-api` Worker kept running — both bound to the same D1 at that point.
- Updated Cloudflare Pages Production env var `VITE_API_URL` to the new Worker URL.
- Triggered a Pages rebuild (retry-latest-deployment).
- Verified via browser DevTools: the `/api/preferences` cross-origin call on `happyhour.day` now hits `happyhour-api.*` with CORS `Access-Control-Allow-Origin: https://happyhour.day` and status 204 on preflight.
- Deleted old `khlock-api` Worker via Cloudflare Dashboard → Settings → Delete.

### B.3 — D1 DB `khlock-db` → `happyhour-db` (full migration, not rename)

D1 doesn't support in-place rename (no dashboard option, no wrangler subcommand). Since the goal was full name consistency, executed a proper export/import migration.

**Steps run autonomously with a pause at step 8 for user verification:**

1. `wrangler d1 export khlock-db --remote --output=/tmp/khlock-db-backup.sql` — 21-line SQL backup (schema + 4 user rows).
2. `wrangler d1 create happyhour-db` → new DB ID `95eec4c7-ccb0-4530-a4c8-acd9e22f13ca`.
3. `wrangler d1 execute happyhour-db --remote --file=/tmp/khlock-db-backup.sql` — 21 rows written, 2 tables, 2.95ms execute time.
4. **Verification:** `COUNT(*)` and a per-row diff (user_id, length(zones), updated_at) matched exactly between old and new DBs across all 4 users.
5. Edited `api/wrangler.jsonc` line 8–9: `database_name` → `happyhour-db`, `database_id` → `95eec4c7-ccb0-4530-a4c8-acd9e22f13ca`.
6. Edited `api/package.json` db scripts to reference `happyhour-db` (3 npm scripts).
7. `wrangler deploy` — Worker now binds `env.DB (happyhour-db)`.
8. User verified cloud sync working on two devices.
9. `wrangler d1 delete khlock-db --skip-confirmation` — old DB permanently removed.

**Re-authed wrangler partway through B.3:** the existing OAuth token returned `code 10000: Authentication error` on D1 API calls despite claiming `d1 (write)` scope. `wrangler logout && wrangler login` fixed it immediately — likely a stale scope-binding on the prior token. Noted in case it happens again.

### B.4 — CORS allowlist cleanup (skipped)

`api/wrangler.jsonc` `ALLOWED_ORIGINS` still contains `https://khlock.pages.dev`. Left intact because the Pages project name is still `khlock`, so the fallback URL `khlock.pages.dev` remains a valid entry point. Zero cost to leave.

### B.5 — Cloudflare Pages project rename (skipped, not supported in-place)

Cloudflare Pages does not allow renaming an existing project. Options would have been (a) leave as-is, or (b) create a new project + migrate custom domain + delete old. Chose (a). The project name is user-invisible once the custom domain is live, and the fallback URL still resolves.

### Security follow-up

`docs/HANDOFF.md` (gitignored, not in git history) contained a GitHub PAT in plain text. The token has been redacted from the local file; user should still revoke it at `github.com/settings/tokens` since the file may have been shared through non-git channels (screenshots, copies). Not urgent since no public exposure was found — the token value is deliberately not reproduced in this devlog so GitHub's push-protection scanner doesn't re-flag it.

## Verification (Phase B)

- `npx wrangler d1 list` shows only `happyhour-db` (old `khlock-db` deleted).
- `npx wrangler deploy` output shows bindings: `env.DB (happyhour-db)`, `env.CLERK_JWKS_URL`, `env.ALLOWED_ORIGINS`.
- Browser DevTools on `happyhour.day`: cross-origin calls hit `happyhour-api.*`, CORS passes, preferences sync on 2 independent devices confirmed.
- Backup SQL preserved at `/tmp/khlock-db-backup.sql` (ephemeral; gets cleaned up by macOS) as a safety net.

## Final state (commit IDs)

- `100faf2` — Tier 1 + Tier 2 (rebrand plumbing + Happy Mode + Figma assets + UX fixes + bug fixes)
- `1b1d2d7` — JWKS URL fix (dev → production Clerk tenant)
- `fe1a8e2` — Phase A (doc + lockfile cleanup)
- *(next commit)* — Phase B (Worker + D1 config updates); GitHub rename itself has no corresponding commit

## Remaining `khlock` references — audit after Phase B

- `api/wrangler.jsonc` `ALLOWED_ORIGINS`: `khlock.pages.dev` — intentional fallback (see B.4)
- Historical dated docs (`2026-03-*-khlock-edits.md`, etc.) — intentional project history
- `docs/PRD.md` factual historical references — intentional
- `.claude/settings.local.json` filesystem paths — will self-resolve if user renames local folder
- `docs/HANDOFF.md` — gitignored, redacted locally

No current-tense brand references remain in any tracked file outside `.gitignore`d artifacts or explicitly historical contexts.

---

## 2252 follow-up edits

Source spec for these was referenced as `docs/2026-04-20-happyhour-edits-2252.md`, but that file does not exist in the repo — only `docs/2026-04-20-happyhour-edits.md` (the original brief) and its `… 2.md` iCloud duplicate are present. Implemented the 8 items directly from the session prompt's specifications, using Figma MCP reads for each referenced node.

### Item 5 — Dark-mode Add City popover border

`client/src/components/digital-clock.tsx` — `CitySelector`'s `PopoverContent` now has `dark:border-[#333333]` appended to the existing `className="w-[280px] p-0 z-[60]"`. Scoped to `dark:` only so light + Happy modes are untouched. Did not modify `--border` token in `index.css` per the instruction to keep the fix narrow.

### Item 1 — Happy Mode header logo variant (Figma 198:1582)

`client/src/components/icons/happyhour-logo.tsx` now accepts an optional `variant?: "default" | "dark" | "happy"` prop. In Happy Mode the yellow `#FFCD05` circle changes to white `#FFFFFF` so the mark stays visible against the `#FFD900` page background (per Figma 198:1582 which renders the smiley on a yellow strip with a light-colored circle). The smile + checkmark paths continue to use `currentColor` so they pick up `text-foreground` unchanged. Call site: `client/src/pages/world-clock.tsx` now reads `resolvedTheme` from `useTheme()` and derives a `logoVariant` string, passed to `<HappyhourLogo variant={logoVariant} />`.

### Item 6 — Dark-mode logo variant (Figma 114:1333)

Extended the variant scheme above to include `"dark"`. The dark Figma node (114:1333) renders the smiley with a yellow circle and a white face — which is what the existing `currentColor` face paths already produce in dark mode, since `--foreground` resolves to near-white (`0 0% 90%`). The `"dark"` branch therefore keeps the yellow circle fill; no SVG duplication is needed. Left a doc comment on the variant type explaining why.

### Item 3 — Sidebar username color

`client/src/components/sidebar.tsx` — swapped the `{firstName}` paragraph's `text-[#efefef]` for `text-[#FFD900]`. Only the first-name span changed; "Welcome back," and the email line stay `#efefef`. This matches the Figma sidebar node (114:1479) which shows the name in `#ffd900`.

### Item 4 — Sidebar Appearance cycle icon (Figma source 95:2615)

Node `203:1347` returned "invalid node" via MCP. Fell back to Figma's canonical `AppearanceIcon` component (node 95:2615, described as "tv, screen, display, virtual machine, vm"), which is the trailing icon on the Appearance row in the checked-in sidebar variants. Downloaded the raw SVG, inlined it as `client/src/components/icons/appearance-icon.tsx` (24×24, `currentColor` fill). `sidebar.tsx` now renders `<AppearanceIcon className="h-[24px] w-[24px]" />` regardless of theme state — the previous per-state `getThemeIcon()` function and its `Monitor/Smartphone/Sun/Moon/HappyhourLogo` imports are removed. `getThemeLabel()` is preserved and supplied as the button's `title` so hover-tooltips still announce the current theme.

### Item 2 — Temperature legibility on yellow

`client/src/index.css` gained a `.happy`-scoped override block that darkens three Tailwind utility classes that lose contrast on `#ffd900`: `.text-yellow-500 → #8b5a00` (brown), `.text-orange-500 → #b45309` (burnt orange), `.text-green-500 → #166534` (deep forest). Cyan, blue, and red already pass AA on yellow so they're unchanged. No JS change — `getTemperatureColor` in `use-weather.ts` still emits the same `text-*-500` classes, and the CSS cascade applies only when `.happy` is on `<html>`.

### Item 7 — Mobile header logo + wordmark sizing (Figma 6:2)

`client/src/pages/world-clock.tsx` — split the old `FS_START = 48` into `FS_START_DESKTOP = 48` and `FS_START_MOBILE = 36`, added `MOBILE_BREAKPOINT = 640` (Tailwind `sm:`), and `updateHeader` now picks the right starting font size based on `window.innerWidth` before interpolating to `FS_END = 30` on scroll. Added a `resize` listener alongside `scroll` so the choice updates on orientation change. The `HappyhourLogo` remains `w-[0.95em]` so it shrinks in lockstep with the wordmark. Mobile left-alignment: the header's `pl-[10px]` and the hero clock's `px-[10px]` already sit inside the same `px-6 md:px-12 lg:px-24` + `max-w-4xl` container, so both edges land at viewport-left + 24 + 10 = 34px on mobile — no layout change needed. **User-eyeball items:** verify the Happyhour wordmark no longer wraps/overflows next to the drawer toggle on iPhone-SE-class widths, and that the smiley + text visually left-align with the "New York" + hero time column below.

### Item 8 — Mobile drawer icon alignment (investigation)

Walked the math in HEAD. Page header button: inside `<header class="sticky top-0 ... py-8">` with inner flex `items-start`; `toggleRef.getBoundingClientRect().top === 32` (py-8 = 32px) when scroll is 0. Sidebar panel is positioned `top: topOffset - 18` on the fixed wrapper, with `pt-[18px]` inside, so its internal close button lands at viewport-y `= (topOffset - 18) + 18 = topOffset`. Both `DrawerClosedIcon` and `DrawerOpenIcon` use the same viewBox (`0 0 25 20`) and share the outer rectangle path (`M20.75,2c.41,0,.75.34.75.75v14…`), so icon geometric centers are identical (x=11.75, y=10 within the 25×20 box). Both buttons have intrinsic size 25×20 (sidebar via explicit `w-[25px] h-[20px]`; page via SVG intrinsic + Tailwind preflight reseting button padding/border to 0). Conclusion: the math and geometry are consistent in HEAD — I did not introduce a structural change. **If the user still sees a ~6px drift, the likely causes are (a) a stale `sidebarTop` React state when the sidebar opens mid-scroll, or (b) a platform-specific sub-pixel rendering difference between the sticky-header button and the absolutely-positioned sidebar button.** A robust follow-up would replace the React-state approach with a CSS variable written directly on `<html>` from the scroll listener (synchronous, no render-frame lag). Flagging but not implementing since that's a structural refactor outside this item's scope.

### Observed but out of scope

- `client/src/pages/world-clock.tsx` line 128 still sets `h1` font-size via inline `style` on every scroll tick. On Happy Mode with a touch device this can thrash paint — acceptable, but a `transform: scale()` approach would avoid reflow. Not touched this session.
- `design/User Interface Designs/` has both a `… 2.md` and original iCloud duplicate for the happyhour-edits brief (same for several other docs). The duplicate-filename pattern from iCloud sync is pervasive across `docs/`; a batch cleanup would reduce noise but risks deleting the wrong version.
- Figma node `203:1347` (referenced in the prompt for item 4) was invalid via MCP. Used the canonical `AppearanceIcon` component `95:2615` instead and flagged it here so the user can confirm or redirect.

### Verification

- `npx tsc --noEmit` — clean (exit 0)
- `npm run build` — clean, bundle sizes unchanged (494.64 KB main, 40.15 KB CSS, 2.05 MB cities chunk)
- **Manual items for the user to verify:**
  - **Mobile (items 7, 8):** On a real iPhone (or Chrome DevTools iPhone SE preset at 375×667), confirm the header wordmark fits on one line next to the drawer toggle, and that opening/closing the sidebar keeps the drawer icon at the same pixel y.
  - **Happy Mode cycle (items 1, 2, 6):** Open sidebar → tap Appearance until the body goes yellow. Confirm (a) the header smiley now has a white circle against yellow, (b) weather temp readings stay legible (yellow/orange ranges should appear brown/burnt-orange), and (c) rotating the theme through dark mode shows a yellow circle with white face.
  - **Sidebar username color (item 3):** When signed in, the first-name line should be Happy-yellow (`#FFD900`) against the dark sidebar background.
  - **Dark Add City border (item 5):** In dark mode, click the city selector inside a clock tile — popover border should be `#333333` instead of the previous brighter stroke.
